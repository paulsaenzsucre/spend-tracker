<%= form_with(model: cat_exp) do |form| %>
  <% if cat_exp.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cat_exp.errors.count, "error") %> prohibited this cat_exp from being saved:</h2>

      <ul>
        <% cat_exp.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :category_id, style: "display: block" %>
    <%= form.text_field :category_id %>
  </div>

  <div>
    <%= form.label :expense_id, style: "display: block" %>
    <%= form.text_field :expense_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
